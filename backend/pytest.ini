[tool:pytest]
asyncio_mode=auto
# 테스트 디렉토리 설정
testpaths = tests

# Python 경로 설정 (app 모듈 인식을 위해)
addopts = --tb=short -v
pythonpath = .

# 테스트 파일 패턴
python_files = test_*.py *_test.py

# 테스트 클래스 패턴
python_classes = Test*

# 테스트 함수 패턴
python_functions = test_*
